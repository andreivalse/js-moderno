const producto = 'Monitor 20 pulgadas';

// .repetear te a a permitir repterir una cadena de texto

const texto = ' en Promoción'.repeat(3);

console.log(producto , texto);
console.log(`${producto} ${texto} !!!`);

console.log(producto.split(" "));

